#include<stdio.h>
int main(){
    int a[100],n,i;
    printf("enter the size of array: ");
    scanf("%d",&n);
    for ( i = 0; i < n; i++)
    {
        printf("\nenter %d element: ",i);
        scanf("%d",&a[i]);
    }
    for ( i = 0; i < n-1; i++)
    {
        if (a[i])
        {
            /* code */
        }
        
    }
    
}