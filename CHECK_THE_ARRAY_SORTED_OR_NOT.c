#include<stdio.h>
int main(){
    int a[20],n,i;
    printf("Enter the size of the array: ");
    scanf("%d",&n);
    for(i=0;i<n;i++){
        printf("\nEnter %d element of an array: ",i);
        scanf("%d",&a[i]);
    }
    a[1]=max
    a[1]=min
    if (a[0]>a[1])
    {
        for ( i = 1; i < n; i++)
        {
            if (a[i]>a[i+1])
            {
                /* code */
            }
            
        }
        
    }
    
}